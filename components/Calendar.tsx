import React from "react";
import ReactCalendar from "react-calendar";

const Calendar = () => {
  return (
    <div>
      <ReactCalendar className="REACT_CALENDAR" view="month" />
    </div>
  );
};

export default Calendar;
